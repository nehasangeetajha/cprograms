#include<stdio.h>
#include<conio.h>
int main()
{
    char ch[30];
    int a=0;
    for(int i=0;ch[i]!='\0';i++)
    {
        ch[i]=getchar();
        if (ch[i]=='a
            ')
            a++;
    }
    printf("%d",a);
    getch;
    return 0;
}
